fixture_name: dirty_data
description: 'Single-table fixture with intentional data quality issues: mixed casing,
  whitespace, pre-redacted placeholders, unicode normalization, empty vs null. Exercises
  validation + sentinel detection.'
files:
- table: messy
  path: messy.csv
  format: csv
relationships: []
invariants_post_mask:
- STORM sentinel detection flags pre-redacted XXX-XX-XXXX placeholders before masking
- empty strings and NULL literals are distinguishable in profiling
- unicode normalization does not change content during mask (NFC stays NFC)
expected_orphans: 0
gating_sprints:
- S10
- S11
- S12
