"""Relationship graph: topologically-ordered DAG of FK relationships.

The graph is the planning-layer data structure that answers two questions
S3-S13 ask repeatedly:

- "What parent tables must mask before this child table can mask?"
  (`children_of` for the upstream direction; `parents_of` for the lookup)
- "What is the deterministic order to walk all relationships?"
  (`ordering` for the topological sort)

Composite parents collapse to one node: child columns wait on the whole
parent tuple, not on individual columns of a composite PK.

Source patterns:

- DAG topological sort follows Kahn's algorithm with sorted-queue
  determinism for byte-stable ordering across Python runtimes (same shape
  S1's `check_fk_plan_ordering` used; this module replaces that inline
  implementation per S2 §4 wiring).
- The immutable-edge + graph-as-frozen-artifact shape draws from dbt's
  `manifest.json` dependency graph (every node + every edge committed
  to the artifact; downstream consumers read, never mutate).
- Multi-parent FK (WS5, 2026-06-12): a child column referencing multiple
  parent tables resolves DECLARED-ORDER FIRST-HIT-WINS through the
  parent maps at execution time (the polymorphic-association pattern);
  a row is an orphan only when absent from every parent map. The one
  declaration error: per-edge orphan policies on a shared child tuple
  must agree (orphan_policy_conflict).

`is_descendant` is intentionally NOT in this module's public surface
(per S2 spec M3 resolution): no concrete consumer in S2-S10 cites it;
narrow interfaces (best-practices §3.3) keep speculative public API out
of the post-GA deprecation surface. `parents_of` + `children_of` cover
the cited S9 execution-ordering use case.
"""

from __future__ import annotations

import heapq
from collections import defaultdict
from dataclasses import dataclass
from enum import Enum
from typing import Any

from decoy_engine.plan._errors import PlanCompileError
from decoy_engine.profile._types import Relationship
from decoy_engine.relationships._namespace import NamespaceRegistry


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict


class OrphanPolicy(Enum):
    """Runtime enum of orphan-FK handling policies.

    String values match `decoy_engine.plan._types.OrphanPolicy` literal
    (the YAML-serializable form) and `tests/fixtures/golden/_manifest_schema`
    `OrphanPolicyLiteral` (the manifest-validation form). The three
    representations are kept in sync by tests that exercise each value
    end-to-end.

    Per S2 spec §3 (resolution of M2): the `remap` policy uses the
    sentinel format `masked_orphan_<id>`; bumping that format is a
    release-notes line per `done-definition.md`.
    """

    PRESERVE = "preserve"
    REMAP = "remap"
    WARN = "warn"
    FAIL = "fail"


@dataclass(frozen=True)
class RelationshipEdge:
    """One resolved FK relationship in the planner-layer graph.

    Mirrors `decoy_engine.profile.Relationship` (the data-shape side) with
    two planner-layer fields added: `namespace` is always non-None at this
    layer (the registry resolved it) and `orphan_policy` is required (the
    `orphan_fk_policy_completeness` check guarantees the config supplied
    one before the graph was built).

    `parent_columns` and `child_columns` are tuples of length 1 for
    single-column FKs and >1 for composite-key FKs. Both tuples must
    have the same length (enforced by the source `Relationship`'s
    `__post_init__`).
    """

    parent_table: str
    parent_columns: tuple[str, ...]
    child_table: str
    child_columns: tuple[str, ...]
    namespace: str
    orphan_policy: OrphanPolicy


@dataclass(frozen=True)
class RelationshipGraph:
    """Frozen DAG of FK relationships + topological ordering.

    `edges` carries every resolved relationship as a `RelationshipEdge`.
    `ordering` is the (table, columns) walk order: parents before children,
    composite parents collapsed to one node. Both are deterministic across
    runs given equal input.

    Query helpers `parents_of` and `children_of` are O(edges) lookups; the
    graph is small enough at typical pipeline sizes (hundreds of tables,
    thousands of edges max) that an index is overkill. If profiling shows
    these as a hotspot in S9, swap the implementation behind the same API.
    """

    edges: tuple[RelationshipEdge, ...]
    ordering: tuple[tuple[str, tuple[str, ...]], ...]

    def parents_of(
        self, child_table: str, child_columns: tuple[str, ...]
    ) -> tuple[RelationshipEdge, ...]:
        """Every edge whose child matches `(child_table, child_columns)`."""
        return tuple(
            e
            for e in self.edges
            if e.child_table == child_table and e.child_columns == child_columns
        )

    def children_of(
        self, parent_table: str, parent_columns: tuple[str, ...]
    ) -> tuple[RelationshipEdge, ...]:
        """Every edge whose parent matches `(parent_table, parent_columns)`."""
        return tuple(
            e
            for e in self.edges
            if e.parent_table == parent_table and e.parent_columns == parent_columns
        )
mutants_x_build_relationship_graph__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_build_relationship_graph__mutmut)
def build_relationship_graph(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_orig(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_1(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = None
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_2(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(None)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_3(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(None)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_4(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = None
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_5(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns) not in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_6(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) >= 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_7(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 2:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_8(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code=None,
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_9(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=None,
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_10(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=None,
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_11(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_12(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_13(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_14(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="XXorphan_policy_conflictXX",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_15(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="ORPHAN_POLICY_CONFLICT",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_16(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(None)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_17(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "XXevery parent map before a row counts as an orphan, so the XX"
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_18(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "EVERY PARENT MAP BEFORE A ROW COUNTS AS AN ORPHAN, SO THE "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_19(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "XXedges must share one policy; align the orphan_policy values.XX"
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_20(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "EDGES MUST SHARE ONE POLICY; ALIGN THE ORPHAN_POLICY VALUES."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_21(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = None
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_22(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = None
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_23(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(None)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_24(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = None
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_25(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_26(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code=None,
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_27(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=None,
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_28(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=None,
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_29(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_30(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_31(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_32(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="XXorphan_fk_policy_missingXX",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_33(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="ORPHAN_FK_POLICY_MISSING",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_34(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "XXin the lookup; check_orphan_fk_policy_completeness must run XX"
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_35(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "IN THE LOOKUP; CHECK_ORPHAN_FK_POLICY_COMPLETENESS MUST RUN "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_36(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "XXbefore build_relationship_graph.XX"
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_37(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "BEFORE BUILD_RELATIONSHIP_GRAPH."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_38(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            None
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_39(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=None,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_40(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=None,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_41(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=None,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_42(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=None,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_43(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=None,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_44(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=None,
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_45(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_46(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_47(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_48(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_49(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_50(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_51(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = None

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_52(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(None)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_53(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = None

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_54(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        None
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_55(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            None,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_56(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=None,
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_57(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_58(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_59(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: None,
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_60(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = None
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_61(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add(None)
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_62(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add(None)

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_63(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = None
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_64(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(None, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_65(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, None)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_66(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_67(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, )
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_68(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 1)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_69(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = None
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_70(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        None
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_71(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = None
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_72(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = None
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_73(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(None)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_74(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] = 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_75(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] -= 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_76(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 2

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_77(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = None
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_78(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d != 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_79(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 1]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_80(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(None)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_81(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = None
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_82(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = None
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_83(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(None)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_84(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(None)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_85(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(None):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_86(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] = 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_87(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] += 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_88(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 2
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_89(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] != 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_90(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 1:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_91(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(None, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_92(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, None)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_93(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_94(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, )

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_95(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) == len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_96(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = None
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_97(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(None)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_98(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d >= 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_99(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 1)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_100(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code=None,
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_101(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path=None,
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_102(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=None,
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_103(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_104(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_105(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_106(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="XXfk_cycleXX",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_107(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="FK_CYCLE",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_108(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="XXrelationshipsXX",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_109(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="RELATIONSHIPS",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_110(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "XXdeterministic mask order.XX"
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_111(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "DETERMINISTIC MASK ORDER."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_112(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=None, ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_113(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=None)


def x_build_relationship_graph__mutmut_114(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(ordering=tuple(ordered))


def x_build_relationship_graph__mutmut_115(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, )


def x_build_relationship_graph__mutmut_116(
    relationships: tuple[Relationship, ...],
    *,
    namespace_registry: NamespaceRegistry,
    orphan_policy_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy],
) -> RelationshipGraph:
    """Build a `RelationshipGraph` from profile relationships + planner inputs.

    The function takes profile-side `Relationship` tuples (the source-data
    facts) plus two planner-side inputs the graph needs to resolve every
    edge to a `RelationshipEdge`:

    - `namespace_registry`: resolved by `build_namespace_registry`;
      every relationship's namespace is looked up via
      `registry.for_relationship(rel)` (raises if unresolvable).
    - `orphan_policy_lookup`: resolved by
      `check_orphan_fk_policy_completeness` (which validates the config
      side of the contract first) and keyed by
      `(parent_table, parent_columns, child_table, child_columns)`.
      Every relationship key must be present in this lookup; the check
      is responsible for populating it. The 4-tuple key (S13-rebaseline
      P1, 2026-06-01) lets same-parent-different-child relationships
      declare different orphan policies without colliding.

    Raises:

    - `NamespaceConfigError(code='namespace_missing')`: a relationship
      cannot resolve a namespace via any of (relationship.namespace,
      parent-column binding, child-column binding).
    - `PlanCompileError(code='orphan_policy_conflict')`: a child
      `(child_table, child_columns)` declares FK relationships to
      multiple parent tables whose orphan policies differ. Multi-parent
      itself is supported (WS5, 2026-06-12): the executor resolves
      declared-order first-hit-wins across the parent maps.
    - `PlanCompileError(code='fk_cycle')`: the FK DAG contains a cycle.
      The error message lists the nodes participating in the cycle.

    Pure function: same `(relationships, namespace_registry,
    orphan_policy_lookup)` produces an equal graph.
    """
    # Multi-parent FK (capability-gaps WS5, 2026-06-12; replaces the
    # S2-era multi_parent_fk_unsupported rejection): a child column-tuple
    # MAY reference multiple parent tables. Resolution semantics live in
    # the executor (declared-order first-hit-wins; orphan iff absent from
    # ALL parents). The one declaration error left: per-edge orphan
    # policies on a shared child tuple must agree -- combining e.g.
    # `remap` with `fail` on one column has no coherent meaning.
    by_child: dict[tuple[str, tuple[str, ...]], list[Relationship]] = defaultdict(list)
    for rel in relationships:
        by_child[(rel.child_table, rel.child_columns)].append(rel)
    for (child_table, child_columns), rels in by_child.items():
        policies = {
            orphan_policy_lookup[
                (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            ].value
            for r in rels
            if (r.parent_table, r.parent_columns, r.child_table, r.child_columns)
            in orphan_policy_lookup
        }
        if len(policies) > 1:
            raise PlanCompileError(
                code="orphan_policy_conflict",
                path=f"relationships[{child_table}.{child_columns}]",
                message=(
                    f"Column {child_table}.{child_columns} declares FK relationships "
                    f"to multiple parents with CONFLICTING orphan policies: "
                    f"{sorted(policies)!r}. A multi-parent child resolves through "
                    "every parent map before a row counts as an orphan, so the "
                    "edges must share one policy; align the orphan_policy values."
                ),
            )

    # Resolve every relationship to a RelationshipEdge.
    edges: list[RelationshipEdge] = []
    for rel in relationships:
        namespace = namespace_registry.for_relationship(rel)
        rel_key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if rel_key not in orphan_policy_lookup:
            # Should not happen: check_orphan_fk_policy_completeness must
            # run before this function and must populate every key. If we
            # land here, it's a wiring bug in compile_plan, not user input.
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Relationship {rel.parent_table}.{rel.parent_columns} -> "
                    f"{rel.child_table}.{rel.child_columns} has no orphan_policy "
                    "in the lookup; check_orphan_fk_policy_completeness must run "
                    "before build_relationship_graph."
                ),
            )
        edges.append(
            RelationshipEdge(
                parent_table=rel.parent_table,
                parent_columns=rel.parent_columns,
                child_table=rel.child_table,
                child_columns=rel.child_columns,
                namespace=namespace,
                orphan_policy=orphan_policy_lookup[rel_key],
            )
        )

    # Dedupe at the source (audit M1): a relationship declared twice in
    # the config produced duplicate edges, doubling out_edges entries
    # and inflating indegree -- topological ORDER stayed correct, but
    # downstream edge/indegree bookkeeping (FK-remap counts) was wrong.
    # Identity is the full edge tuple; the orphan-policy lookup is keyed
    # by the same 4-tuple, so true duplicates are byte-identical here.
    deduped: dict[RelationshipEdge, None] = dict.fromkeys(edges)

    # Sort edges deterministically for stable iteration.
    edges_sorted = tuple(
        sorted(
            deduped,
            key=lambda e: (
                e.parent_table,
                e.parent_columns,
                e.child_table,
                e.child_columns,
            ),
        )
    )

    # Topological sort. Composite parent collapses to one node (the whole
    # column tuple). Kahn's algorithm with sorted-queue tie-breaking for
    # byte-stable ordering.
    nodes: set[tuple[str, tuple[str, ...]]] = set()
    for e in edges_sorted:
        nodes.add((e.parent_table, e.parent_columns))
        nodes.add((e.child_table, e.child_columns))

    indegree: dict[tuple[str, tuple[str, ...]], int] = dict.fromkeys(nodes, 0)
    out_edges: dict[tuple[str, tuple[str, ...]], list[tuple[str, tuple[str, ...]]]] = defaultdict(
        list
    )
    for e in edges_sorted:
        parent_node = (e.parent_table, e.parent_columns)
        child_node = (e.child_table, e.child_columns)
        out_edges[parent_node].append(child_node)
        indegree[child_node] += 1

    # QA-8 F1 (2026-06-01): heapq min-heap replaces the list-based
    # Kahn queue. Pre-fix the queue used `queue.pop(0)` (O(n) per
    # iteration) + `queue.sort()` (O(n log n) per insert), yielding
    # O(n^2 log n) worst-case at large pipeline scales (500+ tables).
    # heapq.heappush + heappop is O(log n); same overall ordering
    # because both produce the lexicographically-smallest topological
    # order. Tuples are sortable directly so no key= needed.
    queue: list[tuple[str, tuple[str, ...]]] = [n for n, d in indegree.items() if d == 0]
    heapq.heapify(queue)
    ordered: list[tuple[str, tuple[str, ...]]] = []
    while queue:
        node = heapq.heappop(queue)
        ordered.append(node)
        for nxt in sorted(out_edges[node]):
            indegree[nxt] -= 1
            if indegree[nxt] == 0:
                heapq.heappush(queue, nxt)

    if len(ordered) != len(nodes):
        cycle_nodes = sorted(n for n, d in indegree.items() if d > 0)
        raise PlanCompileError(
            code="fk_cycle",
            path="relationships",
            message=(
                f"FK relationships form a cycle. {len(cycle_nodes)} nodes are part "
                f"of one or more cycles: {cycle_nodes!r}. Plan cannot determine a "
                "deterministic mask order."
            ),
        )

    return RelationshipGraph(edges=edges_sorted, ordering=tuple(None))

mutants_x_build_relationship_graph__mutmut['_mutmut_orig'] = x_build_relationship_graph__mutmut_orig # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_1'] = x_build_relationship_graph__mutmut_1 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_2'] = x_build_relationship_graph__mutmut_2 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_3'] = x_build_relationship_graph__mutmut_3 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_4'] = x_build_relationship_graph__mutmut_4 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_5'] = x_build_relationship_graph__mutmut_5 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_6'] = x_build_relationship_graph__mutmut_6 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_7'] = x_build_relationship_graph__mutmut_7 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_8'] = x_build_relationship_graph__mutmut_8 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_9'] = x_build_relationship_graph__mutmut_9 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_10'] = x_build_relationship_graph__mutmut_10 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_11'] = x_build_relationship_graph__mutmut_11 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_12'] = x_build_relationship_graph__mutmut_12 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_13'] = x_build_relationship_graph__mutmut_13 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_14'] = x_build_relationship_graph__mutmut_14 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_15'] = x_build_relationship_graph__mutmut_15 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_16'] = x_build_relationship_graph__mutmut_16 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_17'] = x_build_relationship_graph__mutmut_17 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_18'] = x_build_relationship_graph__mutmut_18 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_19'] = x_build_relationship_graph__mutmut_19 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_20'] = x_build_relationship_graph__mutmut_20 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_21'] = x_build_relationship_graph__mutmut_21 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_22'] = x_build_relationship_graph__mutmut_22 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_23'] = x_build_relationship_graph__mutmut_23 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_24'] = x_build_relationship_graph__mutmut_24 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_25'] = x_build_relationship_graph__mutmut_25 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_26'] = x_build_relationship_graph__mutmut_26 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_27'] = x_build_relationship_graph__mutmut_27 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_28'] = x_build_relationship_graph__mutmut_28 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_29'] = x_build_relationship_graph__mutmut_29 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_30'] = x_build_relationship_graph__mutmut_30 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_31'] = x_build_relationship_graph__mutmut_31 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_32'] = x_build_relationship_graph__mutmut_32 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_33'] = x_build_relationship_graph__mutmut_33 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_34'] = x_build_relationship_graph__mutmut_34 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_35'] = x_build_relationship_graph__mutmut_35 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_36'] = x_build_relationship_graph__mutmut_36 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_37'] = x_build_relationship_graph__mutmut_37 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_38'] = x_build_relationship_graph__mutmut_38 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_39'] = x_build_relationship_graph__mutmut_39 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_40'] = x_build_relationship_graph__mutmut_40 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_41'] = x_build_relationship_graph__mutmut_41 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_42'] = x_build_relationship_graph__mutmut_42 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_43'] = x_build_relationship_graph__mutmut_43 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_44'] = x_build_relationship_graph__mutmut_44 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_45'] = x_build_relationship_graph__mutmut_45 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_46'] = x_build_relationship_graph__mutmut_46 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_47'] = x_build_relationship_graph__mutmut_47 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_48'] = x_build_relationship_graph__mutmut_48 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_49'] = x_build_relationship_graph__mutmut_49 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_50'] = x_build_relationship_graph__mutmut_50 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_51'] = x_build_relationship_graph__mutmut_51 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_52'] = x_build_relationship_graph__mutmut_52 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_53'] = x_build_relationship_graph__mutmut_53 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_54'] = x_build_relationship_graph__mutmut_54 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_55'] = x_build_relationship_graph__mutmut_55 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_56'] = x_build_relationship_graph__mutmut_56 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_57'] = x_build_relationship_graph__mutmut_57 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_58'] = x_build_relationship_graph__mutmut_58 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_59'] = x_build_relationship_graph__mutmut_59 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_60'] = x_build_relationship_graph__mutmut_60 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_61'] = x_build_relationship_graph__mutmut_61 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_62'] = x_build_relationship_graph__mutmut_62 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_63'] = x_build_relationship_graph__mutmut_63 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_64'] = x_build_relationship_graph__mutmut_64 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_65'] = x_build_relationship_graph__mutmut_65 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_66'] = x_build_relationship_graph__mutmut_66 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_67'] = x_build_relationship_graph__mutmut_67 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_68'] = x_build_relationship_graph__mutmut_68 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_69'] = x_build_relationship_graph__mutmut_69 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_70'] = x_build_relationship_graph__mutmut_70 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_71'] = x_build_relationship_graph__mutmut_71 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_72'] = x_build_relationship_graph__mutmut_72 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_73'] = x_build_relationship_graph__mutmut_73 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_74'] = x_build_relationship_graph__mutmut_74 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_75'] = x_build_relationship_graph__mutmut_75 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_76'] = x_build_relationship_graph__mutmut_76 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_77'] = x_build_relationship_graph__mutmut_77 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_78'] = x_build_relationship_graph__mutmut_78 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_79'] = x_build_relationship_graph__mutmut_79 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_80'] = x_build_relationship_graph__mutmut_80 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_81'] = x_build_relationship_graph__mutmut_81 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_82'] = x_build_relationship_graph__mutmut_82 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_83'] = x_build_relationship_graph__mutmut_83 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_84'] = x_build_relationship_graph__mutmut_84 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_85'] = x_build_relationship_graph__mutmut_85 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_86'] = x_build_relationship_graph__mutmut_86 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_87'] = x_build_relationship_graph__mutmut_87 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_88'] = x_build_relationship_graph__mutmut_88 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_89'] = x_build_relationship_graph__mutmut_89 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_90'] = x_build_relationship_graph__mutmut_90 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_91'] = x_build_relationship_graph__mutmut_91 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_92'] = x_build_relationship_graph__mutmut_92 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_93'] = x_build_relationship_graph__mutmut_93 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_94'] = x_build_relationship_graph__mutmut_94 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_95'] = x_build_relationship_graph__mutmut_95 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_96'] = x_build_relationship_graph__mutmut_96 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_97'] = x_build_relationship_graph__mutmut_97 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_98'] = x_build_relationship_graph__mutmut_98 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_99'] = x_build_relationship_graph__mutmut_99 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_100'] = x_build_relationship_graph__mutmut_100 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_101'] = x_build_relationship_graph__mutmut_101 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_102'] = x_build_relationship_graph__mutmut_102 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_103'] = x_build_relationship_graph__mutmut_103 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_104'] = x_build_relationship_graph__mutmut_104 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_105'] = x_build_relationship_graph__mutmut_105 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_106'] = x_build_relationship_graph__mutmut_106 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_107'] = x_build_relationship_graph__mutmut_107 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_108'] = x_build_relationship_graph__mutmut_108 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_109'] = x_build_relationship_graph__mutmut_109 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_110'] = x_build_relationship_graph__mutmut_110 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_111'] = x_build_relationship_graph__mutmut_111 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_112'] = x_build_relationship_graph__mutmut_112 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_113'] = x_build_relationship_graph__mutmut_113 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_114'] = x_build_relationship_graph__mutmut_114 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_115'] = x_build_relationship_graph__mutmut_115 # type: ignore # mutmut generated
mutants_x_build_relationship_graph__mutmut['x_build_relationship_graph__mutmut_116'] = x_build_relationship_graph__mutmut_116 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_check_orphan_fk_policy_completeness__mutmut)
def check_orphan_fk_policy_completeness(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_orig(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_1(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = None
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_2(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = None
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_3(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get(None, [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_4(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", None)
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_5(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get([])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_6(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", )
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_7(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("XXrelationshipsXX", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_8(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("RELATIONSHIPS", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_9(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(None):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_10(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_11(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                break
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_12(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = None
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_13(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get(None)
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_14(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("XXparentXX")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_15(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("PARENT")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_16(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_17(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                break
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_18(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = None
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_19(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get(None)
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_20(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("XXtableXX")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_21(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("TABLE")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_22(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = None
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_23(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get(None)
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_24(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("XXcolumnsXX")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_25(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("COLUMNS")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_26(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_27(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list) or all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_28(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str) or isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_29(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(None)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_30(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                break
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_31(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = None
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_32(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get(None)
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_33(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("XXorphan_policyXX")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_34(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("ORPHAN_POLICY")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_35(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is not None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_36(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code=None,
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_37(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=None,
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_38(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=None,
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_39(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_40(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_41(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_42(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="XXorphan_fk_policy_missingXX",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_43(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="ORPHAN_FK_POLICY_MISSING",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_44(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "XXdoes not declare an orphan_policy. Every relationship must XX"
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_45(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_46(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "DOES NOT DECLARE AN ORPHAN_POLICY. EVERY RELATIONSHIP MUST "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_47(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "XXexplicitly name one of: 'preserve', 'remap', 'warn', 'fail'. XX"
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_48(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "EXPLICITLY NAME ONE OF: 'PRESERVE', 'REMAP', 'WARN', 'FAIL'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_49(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "XXThere is no default.XX"
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_50(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "there is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_51(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "THERE IS NO DEFAULT."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_52(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_53(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("XXpreserveXX", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_54(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("PRESERVE", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_55(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "XXremapXX", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_56(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "REMAP", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_57(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "XXwarnXX", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_58(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "WARN", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_59(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "XXfailXX"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_60(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "FAIL"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_61(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code=None,
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_62(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=None,
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_63(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=None,
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_64(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_65(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_66(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_67(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="XXorphan_fk_policy_invalidXX",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_68(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="ORPHAN_FK_POLICY_INVALID",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_69(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "XXfour valid values: 'preserve', 'remap', 'warn', 'fail'.XX"
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_70(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "FOUR VALID VALUES: 'PRESERVE', 'REMAP', 'WARN', 'FAIL'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_71(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = None
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_72(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get(None, [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_73(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", None)
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_74(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get([])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_75(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", )
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_76(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("XXchildrenXX", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_77(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("CHILDREN", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_78(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_79(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                break
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_80(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_81(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    break
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_82(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = None
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_83(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get(None)
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_84(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("XXtableXX")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_85(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("TABLE")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_86(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = None
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_87(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get(None)
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_88(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("XXcolumnsXX")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_89(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("COLUMNS")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_90(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_91(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list) or all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_92(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str) or isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_93(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(None)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_94(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    break
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_95(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = None
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_96(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(None), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_97(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(None))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_98(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup or config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_99(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key not in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_100(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] == policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_101(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code=None,
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_102(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=None,
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_103(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=None,
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_104(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_105(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_106(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_107(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="XXorphan_fk_policy_duplicateXX",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_108(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="ORPHAN_FK_POLICY_DUPLICATE",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_109(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "XXduplicate entry.XX"
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_110(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "DUPLICATE ENTRY."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_111(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = None

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_112(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = None
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_113(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = None
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_114(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_115(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code=None,
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_116(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=None,
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_117(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=None,
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_118(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_119(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_120(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_121(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="XXorphan_fk_policy_missingXX",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_122(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="ORPHAN_FK_POLICY_MISSING",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_123(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "XXbut the config has no matching relationship entry. Every XX"
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_124(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_125(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "BUT THE CONFIG HAS NO MATCHING RELATIONSHIP ENTRY. EVERY "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_126(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "XXrelationship requires an explicit orphan_policy declaration in XX"
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_127(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "RELATIONSHIP REQUIRES AN EXPLICIT ORPHAN_POLICY DECLARATION IN "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_128(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "XXthe config; one of: 'preserve', 'remap', 'warn', 'fail'.XX"
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_129(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "THE CONFIG; ONE OF: 'PRESERVE', 'REMAP', 'WARN', 'FAIL'."
                ),
            )
        lookup[key] = OrphanPolicy(config_lookup[key])
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_130(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = None
    return lookup


def x_check_orphan_fk_policy_completeness__mutmut_131(
    config: dict[str, Any],
    relationships: tuple[Relationship, ...],
) -> dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy]:
    """Validate that the config declares `orphan_policy` for every relationship.

    Per S2 §3 (resolution of TODO 4): no default; every relationship must
    explicitly name its orphan-handling policy. Returns a lookup keyed
    by `(parent_table, parent_columns, child_table, child_columns)` ->
    `OrphanPolicy` so `build_relationship_graph` can populate every
    `RelationshipEdge` without re-reading the config. The 4-tuple key
    (S13-rebaseline P1, 2026-06-01) allows same-parent-different-child
    relationships to declare different orphan policies; pre-fix the
    2-tuple key collapsed them and rejected the legitimate pattern as a
    duplicate.

    Raises:

    - `PlanCompileError(code='orphan_fk_policy_missing')`: a relationship
      exists in `profile.relationships` but the config has no matching
      entry, OR the matching entry has no `orphan_policy` field.
    - `PlanCompileError(code='orphan_fk_policy_invalid')`: the config
      entry declares an `orphan_policy` value not in the four valid
      values (`preserve`, `remap`, `warn`, `fail`).

    Compile-check ownership table row #6 (per S1 spec).
    """
    # Build the lookup from the config side: each (parent, child) pair
    # maps to one orphan_policy string. Reject invalid values immediately
    # so the error names the offending entry's exact location.
    #
    # S13-rebaseline P1 BLOCKER fix (2026-06-01): key is now
    # (parent_table, parent_cols, child_table, child_cols) so two
    # legitimate FKs from the same parent to different children with
    # different orphan policies are not falsely flagged as duplicates.
    # The nullable_fk golden invariant exercises this: an `employees`
    # table is the parent of two `reviews` FKs (employee_id +
    # reviewer_id) with different policies. Pre-fix the (parent_only)
    # dedup key rejected the second entry as a merge conflict; the
    # legitimate pattern was unbuildable.
    config_lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], str] = {}
    config_relationships = config.get("relationships", [])
    if isinstance(config_relationships, list):
        for idx, entry in enumerate(config_relationships):
            if not isinstance(entry, dict):
                continue
            parent = entry.get("parent")
            if not isinstance(parent, dict):
                continue
            parent_table = parent.get("table")
            parent_cols = parent.get("columns")
            if not (
                isinstance(parent_table, str)
                and isinstance(parent_cols, list)
                and all(isinstance(c, str) for c in parent_cols)
            ):
                continue
            policy = entry.get("orphan_policy")
            if policy is None:
                raise PlanCompileError(
                    code="orphan_fk_policy_missing",
                    path=f"relationships[{idx}]",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        "does not declare an orphan_policy. Every relationship must "
                        "explicitly name one of: 'preserve', 'remap', 'warn', 'fail'. "
                        "There is no default."
                    ),
                )
            if policy not in ("preserve", "remap", "warn", "fail"):
                raise PlanCompileError(
                    code="orphan_fk_policy_invalid",
                    path=f"relationships[{idx}].orphan_policy",
                    message=(
                        f"Relationship entry for parent {parent_table}.{parent_cols} "
                        f"declares orphan_policy={policy!r}, which is not one of the "
                        "four valid values: 'preserve', 'remap', 'warn', 'fail'."
                    ),
                )
            # Read the children list to build per-(parent, child) keys.
            # The canonical config schema declares `children:` as a list
            # of child ends (enforced by config/_relationships.py:Field
            # min_length=1 with extra="forbid"); each child becomes its
            # own key, optionally sharing a policy with siblings.
            children = entry.get("children", [])
            if not isinstance(children, list):
                continue
            for child in children:
                if not isinstance(child, dict):
                    continue
                child_table = child.get("table")
                child_cols = child.get("columns")
                if not (
                    isinstance(child_table, str)
                    and isinstance(child_cols, list)
                    and all(isinstance(c, str) for c in child_cols)
                ):
                    continue
                # QA-8 F3 (2026-06-01, S13-rebaseline fix): detect duplicate-
                # key conflict. A second entry for the same
                # (parent_table, parent_cols, child_table, child_cols)
                # with a different orphan_policy used to silently
                # overwrite the first; the operator never saw the conflict
                # and the winning policy was last-declared. A merge-
                # conflict resolution tool could produce this shape.
                # Same-policy duplicates are tolerated (no information
                # loss). LEGITIMATE same-parent-different-child entries
                # with different policies are NOT a duplicate because
                # the key now includes the child end.
                key = (parent_table, tuple(parent_cols), child_table, tuple(child_cols))
                if key in config_lookup and config_lookup[key] != policy:
                    raise PlanCompileError(
                        code="orphan_fk_policy_duplicate",
                        path=f"relationships[{idx}]",
                        message=(
                            f"Relationship for parent {parent_table}.{parent_cols} "
                            f"-> child {child_table}.{child_cols} "
                            f"declares orphan_policy={policy!r} but a previous "
                            f"entry declared {config_lookup[key]!r}. Remove the "
                            "duplicate entry."
                        ),
                    )
                config_lookup[key] = policy

    # Now check every profile relationship has a matching config entry.
    # Lookup key is now per-(parent, child) so different children of the
    # same parent can carry different policies.
    lookup: dict[tuple[str, tuple[str, ...], str, tuple[str, ...]], OrphanPolicy] = {}
    for rel in relationships:
        key = (
            rel.parent_table,
            rel.parent_columns,
            rel.child_table,
            rel.child_columns,
        )
        if key not in config_lookup:
            raise PlanCompileError(
                code="orphan_fk_policy_missing",
                path=(
                    f"relationships[{rel.parent_table}.{rel.parent_columns}->"
                    f"{rel.child_table}.{rel.child_columns}]"
                ),
                message=(
                    f"Profile declares relationship {rel.parent_table}."
                    f"{rel.parent_columns} -> {rel.child_table}.{rel.child_columns} "
                    "but the config has no matching relationship entry. Every "
                    "relationship requires an explicit orphan_policy declaration in "
                    "the config; one of: 'preserve', 'remap', 'warn', 'fail'."
                ),
            )
        lookup[key] = OrphanPolicy(None)
    return lookup

mutants_x_check_orphan_fk_policy_completeness__mutmut['_mutmut_orig'] = x_check_orphan_fk_policy_completeness__mutmut_orig # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_1'] = x_check_orphan_fk_policy_completeness__mutmut_1 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_2'] = x_check_orphan_fk_policy_completeness__mutmut_2 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_3'] = x_check_orphan_fk_policy_completeness__mutmut_3 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_4'] = x_check_orphan_fk_policy_completeness__mutmut_4 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_5'] = x_check_orphan_fk_policy_completeness__mutmut_5 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_6'] = x_check_orphan_fk_policy_completeness__mutmut_6 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_7'] = x_check_orphan_fk_policy_completeness__mutmut_7 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_8'] = x_check_orphan_fk_policy_completeness__mutmut_8 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_9'] = x_check_orphan_fk_policy_completeness__mutmut_9 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_10'] = x_check_orphan_fk_policy_completeness__mutmut_10 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_11'] = x_check_orphan_fk_policy_completeness__mutmut_11 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_12'] = x_check_orphan_fk_policy_completeness__mutmut_12 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_13'] = x_check_orphan_fk_policy_completeness__mutmut_13 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_14'] = x_check_orphan_fk_policy_completeness__mutmut_14 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_15'] = x_check_orphan_fk_policy_completeness__mutmut_15 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_16'] = x_check_orphan_fk_policy_completeness__mutmut_16 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_17'] = x_check_orphan_fk_policy_completeness__mutmut_17 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_18'] = x_check_orphan_fk_policy_completeness__mutmut_18 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_19'] = x_check_orphan_fk_policy_completeness__mutmut_19 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_20'] = x_check_orphan_fk_policy_completeness__mutmut_20 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_21'] = x_check_orphan_fk_policy_completeness__mutmut_21 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_22'] = x_check_orphan_fk_policy_completeness__mutmut_22 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_23'] = x_check_orphan_fk_policy_completeness__mutmut_23 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_24'] = x_check_orphan_fk_policy_completeness__mutmut_24 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_25'] = x_check_orphan_fk_policy_completeness__mutmut_25 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_26'] = x_check_orphan_fk_policy_completeness__mutmut_26 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_27'] = x_check_orphan_fk_policy_completeness__mutmut_27 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_28'] = x_check_orphan_fk_policy_completeness__mutmut_28 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_29'] = x_check_orphan_fk_policy_completeness__mutmut_29 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_30'] = x_check_orphan_fk_policy_completeness__mutmut_30 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_31'] = x_check_orphan_fk_policy_completeness__mutmut_31 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_32'] = x_check_orphan_fk_policy_completeness__mutmut_32 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_33'] = x_check_orphan_fk_policy_completeness__mutmut_33 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_34'] = x_check_orphan_fk_policy_completeness__mutmut_34 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_35'] = x_check_orphan_fk_policy_completeness__mutmut_35 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_36'] = x_check_orphan_fk_policy_completeness__mutmut_36 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_37'] = x_check_orphan_fk_policy_completeness__mutmut_37 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_38'] = x_check_orphan_fk_policy_completeness__mutmut_38 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_39'] = x_check_orphan_fk_policy_completeness__mutmut_39 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_40'] = x_check_orphan_fk_policy_completeness__mutmut_40 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_41'] = x_check_orphan_fk_policy_completeness__mutmut_41 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_42'] = x_check_orphan_fk_policy_completeness__mutmut_42 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_43'] = x_check_orphan_fk_policy_completeness__mutmut_43 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_44'] = x_check_orphan_fk_policy_completeness__mutmut_44 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_45'] = x_check_orphan_fk_policy_completeness__mutmut_45 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_46'] = x_check_orphan_fk_policy_completeness__mutmut_46 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_47'] = x_check_orphan_fk_policy_completeness__mutmut_47 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_48'] = x_check_orphan_fk_policy_completeness__mutmut_48 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_49'] = x_check_orphan_fk_policy_completeness__mutmut_49 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_50'] = x_check_orphan_fk_policy_completeness__mutmut_50 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_51'] = x_check_orphan_fk_policy_completeness__mutmut_51 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_52'] = x_check_orphan_fk_policy_completeness__mutmut_52 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_53'] = x_check_orphan_fk_policy_completeness__mutmut_53 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_54'] = x_check_orphan_fk_policy_completeness__mutmut_54 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_55'] = x_check_orphan_fk_policy_completeness__mutmut_55 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_56'] = x_check_orphan_fk_policy_completeness__mutmut_56 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_57'] = x_check_orphan_fk_policy_completeness__mutmut_57 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_58'] = x_check_orphan_fk_policy_completeness__mutmut_58 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_59'] = x_check_orphan_fk_policy_completeness__mutmut_59 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_60'] = x_check_orphan_fk_policy_completeness__mutmut_60 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_61'] = x_check_orphan_fk_policy_completeness__mutmut_61 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_62'] = x_check_orphan_fk_policy_completeness__mutmut_62 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_63'] = x_check_orphan_fk_policy_completeness__mutmut_63 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_64'] = x_check_orphan_fk_policy_completeness__mutmut_64 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_65'] = x_check_orphan_fk_policy_completeness__mutmut_65 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_66'] = x_check_orphan_fk_policy_completeness__mutmut_66 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_67'] = x_check_orphan_fk_policy_completeness__mutmut_67 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_68'] = x_check_orphan_fk_policy_completeness__mutmut_68 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_69'] = x_check_orphan_fk_policy_completeness__mutmut_69 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_70'] = x_check_orphan_fk_policy_completeness__mutmut_70 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_71'] = x_check_orphan_fk_policy_completeness__mutmut_71 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_72'] = x_check_orphan_fk_policy_completeness__mutmut_72 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_73'] = x_check_orphan_fk_policy_completeness__mutmut_73 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_74'] = x_check_orphan_fk_policy_completeness__mutmut_74 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_75'] = x_check_orphan_fk_policy_completeness__mutmut_75 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_76'] = x_check_orphan_fk_policy_completeness__mutmut_76 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_77'] = x_check_orphan_fk_policy_completeness__mutmut_77 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_78'] = x_check_orphan_fk_policy_completeness__mutmut_78 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_79'] = x_check_orphan_fk_policy_completeness__mutmut_79 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_80'] = x_check_orphan_fk_policy_completeness__mutmut_80 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_81'] = x_check_orphan_fk_policy_completeness__mutmut_81 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_82'] = x_check_orphan_fk_policy_completeness__mutmut_82 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_83'] = x_check_orphan_fk_policy_completeness__mutmut_83 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_84'] = x_check_orphan_fk_policy_completeness__mutmut_84 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_85'] = x_check_orphan_fk_policy_completeness__mutmut_85 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_86'] = x_check_orphan_fk_policy_completeness__mutmut_86 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_87'] = x_check_orphan_fk_policy_completeness__mutmut_87 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_88'] = x_check_orphan_fk_policy_completeness__mutmut_88 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_89'] = x_check_orphan_fk_policy_completeness__mutmut_89 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_90'] = x_check_orphan_fk_policy_completeness__mutmut_90 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_91'] = x_check_orphan_fk_policy_completeness__mutmut_91 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_92'] = x_check_orphan_fk_policy_completeness__mutmut_92 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_93'] = x_check_orphan_fk_policy_completeness__mutmut_93 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_94'] = x_check_orphan_fk_policy_completeness__mutmut_94 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_95'] = x_check_orphan_fk_policy_completeness__mutmut_95 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_96'] = x_check_orphan_fk_policy_completeness__mutmut_96 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_97'] = x_check_orphan_fk_policy_completeness__mutmut_97 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_98'] = x_check_orphan_fk_policy_completeness__mutmut_98 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_99'] = x_check_orphan_fk_policy_completeness__mutmut_99 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_100'] = x_check_orphan_fk_policy_completeness__mutmut_100 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_101'] = x_check_orphan_fk_policy_completeness__mutmut_101 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_102'] = x_check_orphan_fk_policy_completeness__mutmut_102 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_103'] = x_check_orphan_fk_policy_completeness__mutmut_103 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_104'] = x_check_orphan_fk_policy_completeness__mutmut_104 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_105'] = x_check_orphan_fk_policy_completeness__mutmut_105 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_106'] = x_check_orphan_fk_policy_completeness__mutmut_106 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_107'] = x_check_orphan_fk_policy_completeness__mutmut_107 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_108'] = x_check_orphan_fk_policy_completeness__mutmut_108 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_109'] = x_check_orphan_fk_policy_completeness__mutmut_109 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_110'] = x_check_orphan_fk_policy_completeness__mutmut_110 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_111'] = x_check_orphan_fk_policy_completeness__mutmut_111 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_112'] = x_check_orphan_fk_policy_completeness__mutmut_112 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_113'] = x_check_orphan_fk_policy_completeness__mutmut_113 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_114'] = x_check_orphan_fk_policy_completeness__mutmut_114 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_115'] = x_check_orphan_fk_policy_completeness__mutmut_115 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_116'] = x_check_orphan_fk_policy_completeness__mutmut_116 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_117'] = x_check_orphan_fk_policy_completeness__mutmut_117 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_118'] = x_check_orphan_fk_policy_completeness__mutmut_118 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_119'] = x_check_orphan_fk_policy_completeness__mutmut_119 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_120'] = x_check_orphan_fk_policy_completeness__mutmut_120 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_121'] = x_check_orphan_fk_policy_completeness__mutmut_121 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_122'] = x_check_orphan_fk_policy_completeness__mutmut_122 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_123'] = x_check_orphan_fk_policy_completeness__mutmut_123 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_124'] = x_check_orphan_fk_policy_completeness__mutmut_124 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_125'] = x_check_orphan_fk_policy_completeness__mutmut_125 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_126'] = x_check_orphan_fk_policy_completeness__mutmut_126 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_127'] = x_check_orphan_fk_policy_completeness__mutmut_127 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_128'] = x_check_orphan_fk_policy_completeness__mutmut_128 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_129'] = x_check_orphan_fk_policy_completeness__mutmut_129 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_130'] = x_check_orphan_fk_policy_completeness__mutmut_130 # type: ignore # mutmut generated
mutants_x_check_orphan_fk_policy_completeness__mutmut['x_check_orphan_fk_policy_completeness__mutmut_131'] = x_check_orphan_fk_policy_completeness__mutmut_131 # type: ignore # mutmut generated
